/** localStorage 键名常量，hook 层和 panel 层共享，防止魔法字符串散落两处。 */
export const SETTINGS_KEY = '__tmh_settings__'
/** localStorage 键名：暗色模式设置（'system' | 'light' | 'dark'）。 */
export const THEME_KEY = '__tmh_theme__'
/** Shadow DOM 宿主元素 ID，panel 注入和主题切换共用。 */
export const HOST_ID = 'tmap-hooker-host'
